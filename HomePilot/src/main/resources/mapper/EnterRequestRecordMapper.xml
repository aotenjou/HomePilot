<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.manager.mapper.EnterRequestRecordMapper">
    <select id="selectByUserIdAndHomeId" resultType="com.example.manager.entity.EnterRequestRecord">
        SELECT * FROM enter_request_record WHERE user_id = #{userId} AND home_id = #{homeId} AND status = #{status} AND is_deleted = 0
    </select>

    <select id="getLastInsertId" resultType="java.lang.Long">
        SELECT LAST_INSERT_ID()
    </select>

    <select id="selectAllByHomeId" resultType="com.example.manager.DTO.ViewEnterRequest">
        SELECT r.id AS requestId, r.user_id AS userId, u.username AS userName, r.status AS status, r.record_time AS recordTime
        FROM enter_request_record r
        JOIN user u ON r.user_id = u.id
        WHERE r.home_id = #{homeId} AND r.is_deleted = 0 AND u.is_deleted = 0
    </select>

    <update id="updateStatus">
        UPDATE enter_request_record SET status = #{status} WHERE id = #{requestId}
    </update>

    <select id="selectStatus" resultType="java.lang.Integer">
        SELECT status FROM enter_request_record WHERE id = #{requestId}
    </select>
</mapper>