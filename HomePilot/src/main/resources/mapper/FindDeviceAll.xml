<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.manager.mapper.SceneMapper">
    <insert id="createScene">
        INSERT INTO scene(home_id, user_id, name, end_time, description, status) VALUES (#{homeId}, #{userId}, #{name}, #{endTime}, #{description}, #{status})
    </insert>

    <select id="selectById" resultType="com.example.manager.entity.Scene">
        SELECT * FROM scene WHERE id = #{id} AND is_deleted = 0
    </select>
</mapper>