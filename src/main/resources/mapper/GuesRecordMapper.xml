<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.manager.mapper.GuestRecordMapper">
    <insert id="insertRecord">
        INSERT INTO Guest_Record (home_id, user_id, type, record_time) VALUES (#{homeId}, #{userId}, #{recordType}, #{recordTime})
    </insert>
</mapper>