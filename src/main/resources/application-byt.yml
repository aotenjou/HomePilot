server:
  port: 8080  # 确保云服务器安全组已开放此端口

jwt:
  secret-key: 'lingganguliguliguliguajialinggangulinggangu'
  expiration-time: '86400000'

spring:
  application:
    name: manager

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.130.88.98:3306/manager

    username: manager
    password: '123321'

  jpa:
    hibernate:
      ddl-auto: update  # 生产环境建议改为 validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect  # 显式指定方言

  mqtt:
    url: tcp://117.72.63.151:1883
    username: admin
    password: public
    client:
      id: client-id-${random.uuid}  # 建议增加随机后缀避免重复
      providerid: provider-id
      consumerid: consumer-id
    default:
      topic: topic

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0

ark:
  api:
    key: 05962d59-c5f4-4150-be75-f12f05e5d3d4
    model: doubao-seed-1-6-flash-250615
    temperature: 0.7
    max_tokens: 1024

logging:
  level:
    com.example.homework.mapper: debug